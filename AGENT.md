Project: JARVIS

Rules:
- Keep Flask routes stateless
- JSON-only API responses
- Avoid blocking operations in request handlers

Structure:
- server.py main entry
- routes/ contains endpoints
- utils/ helper functions

Notes:
- Keep changes minimal unless requested
